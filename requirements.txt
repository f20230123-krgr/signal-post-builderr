pydantic==2.11.7
httpx==0.28.1
extruct==0.18.0
trafilatura==2.2.0
rapidfuzz==3.14.6
pytest==9.1.1
pytest-asyncio==1.4.0

# Optional: only needed for the Playwright JS-shell fallback in crawl.py
# (invoked lazily via an injected render_js callable -- its absence degrades
# gracefully rather than failing). Not required to run the test suite.
# playwright==1.55.0
